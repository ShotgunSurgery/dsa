#include <iostream>
#include <stack>

using namespace std;

int main(){
    stack<int> st;
    st.push(1);
    st.push(2);
    st.push(3);
    st.push(4);
    st.push(5);

    // stack<int> st_cpy(st);
    // st_cpy.pop();
    // int i = st_cpy.top();
    // st_cpy.pop();
    // cout << i << endl;

    // int n = 3;
    // for(int i = 0; i < n - 1; i++){
    //     st.pop();
    // }
    // int output = st.top();
    // st.pop();
    // cout << output;

    // int i;
    // while(st.top()){
    //     i = st.top();
    //     st.pop();
    // }
    // cout << i;

    stack <int> st_cpy;
    
}