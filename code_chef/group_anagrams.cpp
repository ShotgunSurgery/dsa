#include <iostream>
#include <vector>
using namespace std;

class Solution {
  public:
    vector<vector<string>> groupAnagrams(const vector<string>& strs){
      
    }
};

int main(){
    int no_strings;
    vector<string> vector;
    for(int i = 0; i < no_strings; i++){
        char str[10];
        cin >> str;
        vector.push_back(str);
    }
    Solution s;
    vector<vector<string>> = s.groupAnagrams(vector);
}